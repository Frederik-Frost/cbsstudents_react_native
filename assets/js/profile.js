export class Profile {
    constructor( text ) {
        this.id = id; // Get from firebase user uid
        this.firstName = firstName;
        this.lastName = lastName;
        this.profileImgUrl = profileImgUrl
        this.studyProgramme = this.studyProgramme
    }
}